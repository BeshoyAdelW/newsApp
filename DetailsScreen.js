import React from "react";
import { View, StyleSheet } from "react-native";

function DetailsScreen(props) {
  return <WebView url={this.state.url} />;
}

const styles = StyleSheet.create({
  container: {},
});
export default DetailsScreen;
